## Keep this empty when using the Vite /api proxy or a reverse proxy.
## Set it only when the backend runs on a different origin without a proxy.
## Do not include a trailing slash.
VITE_API_BASE_URL=
